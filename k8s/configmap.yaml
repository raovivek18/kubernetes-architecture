apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
  namespace: kubeops
data:
  DB_HOST: postgres-service.kubeops.svc.cluster.local
  DB_PORT: "5432"
  DB_NAME: kubeops_db
  DB_USER: postgres_user
  APP_VERSION: 1.0.0
